<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CapiFlow</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">

  <style>
    body {
      background-color: #004778;
      font-family: 'Lato', sans-serif;
    }

    .login-container {
      background: rgba(255, 255, 255, 0.8);
      border-radius: 1.5rem;
    }

    .login-bg {
      min-height: 100vh;
    }

    .btn-custom {
      background-color: #004778;
      color: white;
      border: none;
      transition: background-color 0.3s ease;
    }

    .btn-custom:hover {
      background-color: #005d9b;
    }

    .input-group-text {
      background-color: #004778;
      color: white;
      border: none;
    }

    input.form-control {
      font-size: 0.9rem;
    }

    @media (max-width: 768px) {
      .login-container {
        width: 90% !important;
      }
    }
  </style>
</head>
<body class="d-flex justify-content-center align-items-center p-3 login-bg">
  
  <div class="login-container rounded-4 d-flex flex-column align-items-center p-3 p-md-4 w-100" style="max-width: 400px;">
    <div class="mt-3 h5"><strong>Login</strong></div>
    
    <form class="d-flex flex-column align-items-center my-3 w-100" action="/usuario/iniciar-sesion" method="POST">
      <div class="input-group mb-3 rounded-4">
        <span class="input-group-text rounded-start-4">
          <i class="bi bi-person-fill"></i>
        </span>
        <input type="text" name="id" class="form-control rounded-end-4 border-0" placeholder="ID" required>
      </div>

      <div class="input-group mb-3 rounded-4">
        <span class="input-group-text rounded-start-4">
          <i class="bi bi-key-fill"></i>
        </span>
        <input type="password" name="password" class="form-control rounded-end-4 border-0" placeholder="Password" required>
      </div>

      <div class="d-flex flex-column flex-md-row mt-3 align-items-center w-100">
        <input 
          class="rounded-4 btn btn-custom px-4 ms-auto" 
          style="font-size: 0.8rem;" 
          type="button" 
          value="Iniciar Sesión"
          onclick="window.location.href='/investor/marketplace';">
      </div>

    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

